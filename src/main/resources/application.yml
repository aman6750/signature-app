server:
  port: 8081
  tomcat:
    basedir: ./tomcat-temp

spring:
  application:
    name: signature-app


  datasource:
    url: jdbc:mysql://localhost:3306/signature_app
    username: root
    password: aman6750
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB


jwt:
  secret: ThisIsMySuperSecretKeyForJWTTokenGenerationMustBeLongEnoughForHS256
  expiration: 86400000

file:
  upload-dir: ./uploads